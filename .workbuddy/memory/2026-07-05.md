# 2026-07-05 工作记录

## 口算小达人 — 飞牛部署 + 项目完整优化

### 完成内容
1. **Stage 1: 基础设施**
   - 创建 Dockerfile、docker-compose.yml、nginx.conf（缓存控制/Gzip/安全头）
   - 创建 .github/workflows/release.yml（tag 触发自动构建 Release）
   - 修复 generate-icons.js（sharp 生成 SVG + PNG）
   - 新增 autoUpdate 模块（GitHub Releases 检查 + 更新横幅 + SW 通知）
   - 修复 service-worker.js（stale-while-revalidate 策略、动态缓存清理）
   - 修复原代码 bug（levelId === 6 硬编码 → LEVELS.length, i <= 6 → i <= LEVELS.length）

2. **Stage 2: 多孩子档案**
   - store 扩展：getGlobal/setGlobal/getForProfile/setForProfile
   - schema v3→v4 迁移：检测旧数据 → 创建默认 profile → 迁移到 `hx.{pid}.xxx`
   - profiles 模块：CRUD + 数据隔离读写
   - Profile UI：选择器 + 创建/切换/删除
   - 所有现有页面改为 profiles.get/set

3. **Stage 3: 智能难度调节**
   - smartDifficulty 模块：滑动窗口 30 题、动态超时（3-30s）、趋势判断、自适应建议
   - 集成到训练流程：startTimeout/handleCorrect/handleWrongFirst/handleTimeout

4. **Stage 4: 游戏化激励**
   - gamification 模块（~200行）：连击系统、XP/等级、每日任务、15个成就、日历年
   - 集成到 handleCorrect/handleWrongFirst/handleLevelPassed
   - 成就独立页面
   - Home 页展示 XP 条、streak、任务进度

5. **Stage 5: 家长看板增强**
   - reports 模块：弱项分析、周报、SVG 折线图/柱状图
   - 家长面板：智能分析区域、弱项图表、准确率趋势
   - CSV 导出（含时间戳/关卡/正确性/反应时间）
   - 家长面板切换孩子

6. **Stage 6: 打磨**
   - 文档更新（README / DATA-MODEL / ARCHITECTURE）
   - JS 语法验证通过

### 文件变化
- index.html: 2794→4051 行 (+1257)
- 新增：Dockerfile, docker-compose.yml, nginx.conf, .github/workflows/release.yml, .github/release-template.md
- 修改：service-worker.js, manifest.json, generate-icons.js, package.json, README.md, docs/*

### 后续 Bug 修复
- 403 Forbidden：Nginx try_files $uri/ 导致目录列示失败，改为 /index.html 回退
- Docker 权限：Dockerfile 增加 RUN chmod -R 755 /usr/share/nginx/html 解决 COPY 权限不足
- Docker 镜像源：docker.1ms.run 替代损坏的 docker.fnnas.com
- 离开训练页定时器未清理：router.go() 增加自动 clearTrainingTimeout()
- 新增 PWA 添加到主屏幕引导（仅 iOS 首次访问提示一次）
